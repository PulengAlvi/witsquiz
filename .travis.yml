language: node_js
node_js:
  - 19.8.0

cache: npm

install: 
  - npm ci

script:
  - npm test
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB-TOKEN
  on:
    branch: main
